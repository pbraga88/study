Writing Data
============
The write() function, writes to a function up to 'count' bytes from the buffer starting at 'buf' to the file referred to by the file descriptor:
SYNOPSIS
       #include <unistd.h>

       ssize_t write(int fd, const void *buf, size_t count);